# Project-local npm cache.
# Without this, npm resolves its cache from HOME; when HOME is unset (common in
# containers / CI / Joule build agents) it falls back to /.npm/_cacache at the
# filesystem root, which is not writable and makes `npm install` fail hard.
# Keeping the cache inside the project makes the app self-contained.
cache=./.npm-cache
